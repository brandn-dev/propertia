import { BrandedLoadingScreen } from "@/components/loading/branded-loading-screen";

export default function Loading() {
  return (
    <BrandedLoadingScreen
      label="Opening Propertia"
      description="Checking access, restoring your theme, and preparing the next screen."
    />
  );
}
